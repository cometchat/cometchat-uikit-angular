import { CometChat } from "@cometchat-pro/chat"

export interface InputData {
    id?: boolean| null
    thumbnail?: boolean| null,
    status?: boolean| null,
    title?: boolean| null,
    subtitle?: any,
}